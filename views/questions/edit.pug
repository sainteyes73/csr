extends ../layout

block content
  .container
    h1.main-title 질문 수정
    form(action=`/questions/${question.id}?_method=PUT`, method="POST")
      input(type="hidden", name="_method", value="PUT")
      include _form
      button.btn.btn-outline-primary(type="submit") 재등록
