.content!= question.noticeContent

.row


  .col-sm
    .author
      | upload on #{moment(question.createdAt).format('YYYY-MM-DD')}
      if question.author
        | 
        | 요청자 #[a(href=`/users/${question.author.id}`)= question.author.name] 
  .col-sm
    .author
      if question.manager
        |
        | CSR 담당자 #[a(href=`/users/${question.manager.id}`)= question.manager.name]
        
        
